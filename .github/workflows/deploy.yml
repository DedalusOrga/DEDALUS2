name: Deploy to GitHub Pages

on:
  push:
    branches:
      - develop

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Code holen
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Node installieren
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: app/package-lock.json

      # 3. Dependencies installieren (IM app-Ordner!)
      - name: Install dependencies
        working-directory: app
        run: npm ci

      # 4. Build ausführen (IM app-Ordner!)
      - name: Build
        working-directory: app
        run: npm run build

      # 5. app/dist für Pages bereitstellen
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: app/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      # 6. Deployment auf GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
